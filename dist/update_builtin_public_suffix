#!perl

use strict;
use warnings;

use FindBin ();
use lib "$FindBin::Bin/../lib";

use IO::Socket::SSL::PublicSuffix ();

exit __PACKAGE__->run(@ARGV) unless caller();

sub run {
    my ( $self, $url ) = @_;

    IO::Socket::SSL::PublicSuffix::update_self_from_url( $url, 'BuiltIn' );

    return 0;
}

=head1 NAME

update_builtin_public_suffix - Update IO::Socket::SSL's PublicSuffix BuiltIn list

=head1 SYNOPSIS

B<update_builtin_public_suffix> [url]

=head1 DESCRIPTION

This module will create a new IO::Socket::SSL::PublicSuffix::BuiltIn from the 
Public Suffix List at https://publicsuffix.org/list/effective_tld_names.dat
unless another url is specified.

This module is only intended for use by the IO::Socket::SSL maintainers.

=cut

